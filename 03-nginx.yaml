- name: Nginx 
  hosts: local 
  tasks:
  - name: install nginx 
    ansible.builtin.package:
      name: nginx 
      state: present
  - name: start and enable Nginx 
    ansible.builtin.service:
      name: nginx 
      state: started 
      enabled: yes