- name: Nginx 
  hosts: local 
  become: yes 
  tasks:
  - name: install nginx 
    ansible.builtin.package:
      name: nginx 
      state: present
  - name: start and enable Nginx 
    ansible.builtin.service:
      name: nginx 
      state: started 
      enabled: yes